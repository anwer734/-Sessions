services:
  - type: web
    name: telegram-manager
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    envVars:
      - key: SESSION_SECRET
        generateValue: true
